# MVP-R-002 experiment design (qualification only)

Return only `USE_FROZEN_BINDING`. Deterministic code owns all dataset windows,
splits, embargo, controls, metrics, stopping rules, and experiment requests.
Do not propose trading, strategy, future, activation, or governance actions.
